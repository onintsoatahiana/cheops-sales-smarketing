import Link from "next/link"; import {clients} from "@/lib/seed"; import {AppShell} from "@/components/AppShell"; import {Card,Badge} from "@/components/ui";
export default function Clients(){return <AppShell><h2 className="mb-6 text-2xl font-black">Fiches clients 360°</h2><div className="grid gap-4 md:grid-cols-2">{clients.map(c=><Link href={`/clients/${c.id}`} key={c.id}><Card><div className="flex justify-between gap-3"><div><h3 className="font-bold">{c.name}</h3><p className="text-sm text-slate-600">{c.sector} · {c.revenue}</p></div><Badge>{c.region}</Badge></div><p className="mt-3 text-sm">Solutions : {c.solutions.join(" · ")}</p></Card></Link>)}</div></AppShell>}